# Runtime + dev dependencies for the Bear Meter data pipeline.
#
# The model is a single scikit-learn Random Forest — no torch, no XGBoost.
# (The old PyTorch MLP was removed: measured honestly it scored worse than the
# RF, and XGBoost lost to the RF on every config for this calendar-lookup task.)
supabase
pandas
numpy
scikit-learn
requests
pytz
cryptography
httpx[http2]
pytest
