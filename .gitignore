.env
__pycache__/
.pytest_cache/
.claude/
*.pdf
*.pyc

# Local SQLite — no longer committed; Supabase is source of truth
gym_history.db

# Pre-built static data — no longer generated locally
docs/data.json

# Model caches replaced by Supabase tables — moved to legacy/ 2026-07-21
legacy/predictions_cache.json
legacy/weekly_cache.json

# Evaluation-harness output. Regenerated by `python3 backtest.py`; the numbers
# that matter are written up in handoffs/, not committed as raw JSON.
backtest_report.json
